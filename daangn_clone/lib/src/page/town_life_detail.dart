import 'package:flutter/material.dart';

class TownLifeDetail extends StatelessWidget {
  const TownLifeDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
